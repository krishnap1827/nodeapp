Steps to Run Node JS Application:

    1. Create Database in Postgresql with Name "usermaintenance"
    2. After cloning the repository do npm install
    3. To start application enter commands npm run start or npm run dev